# Diploma Domain Pack

Purpose
- Extract holder name, institution, degree type, dates, and certification hints.
- Provide lightweight regex rules for diploma/degree documents.

Files
- `rules.yaml`: regex rules for common fields
- `plugins.py`: heuristic extractors
- `sample_docs/`: optional PDFs (ignored by git)
- `expected/`: expected outputs for regression tests
