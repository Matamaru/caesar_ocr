# Company Universe (Example Schema)

This is a standalone, example SQLite schema for a company with customers,
employees, services, documents, and instances (Belege). It is separate from
core OCR/LayoutLM functionality.

- `db/schema.sql`: schema
- `db/repo.py`: minimal init helper
